{
  "name": "lottery",
  "version": "1.0.0",
  "description": "Test task",
  "main": "index.js",
  "scripts": {
    "migrate-rinkeby": "truffle migrate --network rinkeby --reset -f 1 --to 1",
    "verify-rinkeby": "truffle run verify Lottery --network rinkeby",
    "migrate-kovan": "truffle migrate --network kovan --reset -f 1 --to 1",
    "verify-kovan": "truffle run verify Lottery --network kovan",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maksym Fedorenko",
  "license": "MIT",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "@nomiclabs/hardhat-waffle": "^2.0.0",
    "@truffle/hdwallet-provider": "^2.0.0",
    "chai": "^4.2.0",
    "ethereum-waffle": "^3.0.0",
    "ethers": "^5.0.0",
    "hardhat": "^2.8.3"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.3.1",
    "@openzeppelin/contracts": "^4.4.2",
    "dotenv": "^15.0.0",
    "solidity-coverage": "^0.7.18",
    "truffle-plugin-verify": "^0.5.21"
  }
}
